<% @games.each do |game| %>

<%= link_to game_path(game.id) do %>
<%=  game.title %>
<% end %>

<%= link_to game_path(game.id) do %>
<%= image_tag game.image.thumb.url %>
<% end %>

<%= game.introduction %>

<%= game.genre.genre_name %>


<% end %>


<div class="container">
    <div class="row">
        <div class="col-sm-6">
            うんこ
        </div>
        <div class="col-sm-6">
            ちんこ
        </div>

    </div>
</div>