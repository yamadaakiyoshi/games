<div class="container">
    <div>
        <h1>マイページ</h1>
    </div>
    <div class="row">
        <div class="col-sm-3">
            <div align="center">
                <%# <%= attachment_image_tag @user, :image, :fill,200,200, format: 'jpeg', size: "200x200", fallback: "/images/no-image.png" %>
                %>
            </div>
            <div class="name_display">
                <div align="center">
                    <%= @user.nickname %>
                </div>
            </div>



            <%= link_to "編集",edit_user_path(@user.id), class: "btn btn-success edit_btn" %>
            <%= link_to "退会", user_registration_path, method: :delete, class: "btn btn-success edit_btn" %>